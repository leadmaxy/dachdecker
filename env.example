# Environment variables for local development and Vercel deployment

# Database URL (PostgreSQL) — required if using DB-backed storage
# Format: postgres://user:password@host:port/database
# Example: postgres://postgres:password@localhost:5432/myapp
DATABASE_URL=postgres://user:password@localhost:5432/dbname

# Session secret — used to sign session cookies
# For production, use a strong random string (e.g., from `openssl rand -hex 32`)
SESSION_SECRET=dev-session-secret

# Server port (optional; defaults to 5000)
PORT=5000

# Node environment
NODE_ENV=development